# PostgreSQL (for PostHog internal storage)
POSTGRES_USER=posthog
POSTGRES_PASSWORD=changeme
POSTGRES_DB=posthog

# Redis (PostHog's internal cache/queue — no credentials needed in default setup)

# PostHog App Settings
POSTHOG_SECRET_KEY=replace_with_a_secure_random_key
POSTHOG_SITE_URL=http://localhost:8000
